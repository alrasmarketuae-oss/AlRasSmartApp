<?xml version="1.0" encoding="utf-8"?>
<!--
  Declares trusted HTTPS hosts for API + CDN media.
  cleartext (http) is denied by default; production traffic must use https.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">alrasmarketapp.com</domain>
        <domain includeSubdomains="false">cdn.alrasmarketapp.com</domain>
        <domain includeSubdomains="false">api.alrasmarketapp.com</domain>
        <domain includeSubdomains="false">dashboard.alrasmarketapp.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Local API / emulator only (isLocal builds). -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">192.168.68.63</domain>
    </domain-config>
</network-security-config>
